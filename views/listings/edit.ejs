<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TRAVELNEST</title>
</head>
<body>
    <h3>Create a new list:</h3>
    <form method="POST" action="/listings">
        <label for="title">Title:</label>
        <input type="text" value=" <%= listing.title %>" id="title" name="title" required><br>
        <br>
        <label for="description">Description:</label>
        <textarea id="description" value="<%= listing.description %>" name="description" required></textarea><br>
        <br>
        <input name="listing[image]"
            type="text"
            id="image"
            value="<%= listing.image %>"
            placeholder="Image URL"
            /><br>
        <br>

        <label for="price">Price:</label>
        <input type="number" value="<%= listing.price %>" id="price" name="price" required><br>
        <br>
        <label for="location">Location:</label>
        <input type="text" value="<%= listing.location %>" id="location" name="location" required><br>
        <br>
        <label for="country">Country:</label>
        <input type="text" value="<%= listing.country %>" id="country" name="country" required><br>
        <br>
        <button type="submit">Add</button>

    </form>
</body>
</html>